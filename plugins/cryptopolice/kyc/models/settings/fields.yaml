# ===================================
#  Form Field Definitions
# ===================================

fields:
    settings_label:
      label: Face Api Settings
      type: section

    subscription_key:
          label:  Subscription Keys
          type: text
          required: true
          
    location:
        label: Location
        type: text
        required: true
        comment: You must use the same location in your REST call as you used to obtain your subscription keys.
